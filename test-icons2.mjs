import * as icons from '@hugeicons/core-free-icons';
const names = Object.keys(icons);
console.log(names.filter(n => n === 'Download01Icon' || n === 'Download02Icon' || n === 'ArrowDown01Icon' || n === 'Wallet02Icon' || n === 'BankIcon' || n === 'Activity01Icon' || n === 'Coins01Icon' || n === 'SafeIcon').join(', '));
